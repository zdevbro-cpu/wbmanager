# wbmanager 시스템 — data 폴더 분석 및 기술스택 (초안)

> 작성일: 2026-07-09
> 상태: **초안 — 실제 구현 착수 전 사용자 승인 필요** (CLAUDE.md Phase 1 사전 승인 절차)

---

## 1. data 폴더 분석 요약

`data/` 폴더는 원방(Wonbang) — 스크랩/폐기물 매입·매각 및 해체 사업 — 의 기존 수작업/엑셀/이카운트(ecount) 기반 업무를 정리한 요구사항 자료입니다. 핵심 도면(drawio)과 대표이사 보고서를 분석한 결과는 다음과 같습니다.

### 1-1. 데이터 흐름 (DFD)
원천 데이터(계근/계량증명서, ecount 구매·판매, 올바로 폐기물 신고, 현장 공수표) → Excel 원장 → ETL(입고정규화 → 출고정규화 → 폐기물처리 → 재고이동 반영 → 공수 unpivot → 재고원장 산출 → 손익집계) → 산출물(일일 출고보고, 재고현황, 대표이사 보고, 세금계산서 연동).

### 1-2. 핵심 엔티티 (프로젝트정산 ERD 기준)
- **기준정보**: `item_master`(품목), `project`(차수), `worker`(작업자)
- **물량 트랜잭션**: `inbound`(반입/계근), `sorting`(선별/분류), `inventory_ledger`(재고원장), `outbound_sale`(매각/반출), `waste_outbound`(폐기물 반출)
- **비용 트랜잭션**: `transport`(운송비), `labor`(인건비 일집계), `worker_attendance`(공수출역)
- 모든 트랜잭션은 `project`(차수)를 허브로 1:N 관계이며, 손익 = 실현손익(매각수입−비용) + 재고평가(미실현)

### 1-3. 업무 프로세스 (업무흐름도)
기준정보 등록 → 입고(구매, 계근·계량증명서) → 재고관리 → 출고(판매, 감량확인) → 폐기물처리(올바로) → 자회사 출고(투플러스/유리하이텍/크로스특수) → 일일보고 → 회계정산(매출전표/계산서 발행)

### 1-4. 총무 영역
법인차량·중장비·어테치 이동/정비/검사이력 관리, 임직원·자격증·교육 관리, 도시락 주문 관리

### 1-5. 파일/증빙 특성
계량증명서(PDF/이미지), 현장사진, 상차확인 사진, 정비명세서 등 **증빙 파일이 트랜잭션에 필수로 결합**됨 → DB에는 메타데이터(프로젝트ID, 거래ID, 촬영일, 구분)만 저장하고 실 파일은 **Google Drive**에 저장 후 파일 ID/링크만 참조하는 방식이 요청사항(데이터=CloudSQL, 자료=드라이브)과 일치.

---

## 2. 제안 시스템 아키텍처

```
[클라이언트: React SPA] 
        │  HTTPS
        ▼
[Firebase Hosting] ── 정적 프론트엔드 배포
        │  REST/HTTPS API 호출
        ▼
[Cloud Run 또는 Cloud Functions (Node.js/Express API)]
        │                              │
        ▼                              ▼
[Cloud SQL for PostgreSQL]      [Google Drive API]
  (거래/재고/정산 등 구조화 데이터)   (계량증명서·사진·정비명세서 등 원본 파일,
                                    DB에는 fileId/링크만 저장)
        │
        ▼
[Firebase Authentication] — 사용자 인증/권한(기준정보 "사용자등록·권한설정" 대응)
```

- Firebase Hosting은 정적 파일만 서빙하므로 Cloud SQL에 직접 접속 불가 → **API 서버 계층**(Cloud Run 권장: Cloud SQL Auth Proxy 내장 지원, 오토스케일)이 필요합니다.
- Google Drive 저장은 서비스 계정(또는 공유 드라이브) 기반 업로드로 처리하고, DB에는 Drive fileId만 저장합니다.

---

## 3. 기술스택 제안

| 영역 | 기술 | 비고 |
|---|---|---|
| 프론트엔드 | React + Vite + TypeScript | 기존 다른 사내 프로젝트(itbookmanager 등)와 스택 통일 |
| UI | Tailwind CSS (또는 기존 사용 라이브러리) | data 폴더의 대시보드/현황판 요구 대응 |
| 백엔드 API | Node.js + Express (또는 Fastify) on **Cloud Run** | Cloud SQL Auth Proxy 연동, REST API |
| 데이터베이스 | **Cloud SQL for PostgreSQL** | ERD 기반 정규화 스키마 (project, item_master, inbound, outbound_sale, inventory_ledger, transport, labor, worker_attendance 등) |
| 파일 저장 | **Google Drive API** (서비스 계정 + 공유 드라이브) | 계량증명서/사진/정비명세서 원본, DB엔 메타데이터+fileId만 |
| 인증 | Firebase Authentication | 사용자등록/권한설정 요구사항 대응 |
| 배포/호스팅 | Firebase Hosting (프론트) + Cloud Run (API) | firebase_data.txt에 등록된 프로젝트: `crosswb-a7083` |
| CI/CD | GitHub Actions → Firebase Hosting / Cloud Run 배포 | 저장소: `https://github.com/zdevbro-cpu/wbmanager.git` (사용자 제공, 연결 여부는 별도 확인 필요) |
| ORM | Prisma (PostgreSQL) | 스키마 마이그레이션 관리 용이 |

---

## 4. 참고 사항

- 사용자가 알려주신 Git 저장소(`https://github.com/zdevbro-cpu/wbmanager.git`)는 참고로 기록만 해두었으며, **git init/remote 연결 등 실제 Git 작업은 명시적 요청 시에만 진행**합니다 (CLAUDE.md/agent.md 규칙).
- `firebase_data.txt`의 Firebase 프로젝트(`crosswb-a7083`)를 그대로 사용할지, wbmanager 전용 신규 Firebase 프로젝트를 생성할지 확인이 필요합니다.
- 이카운트(ecount) 연동을 유지할지, wbmanager가 이를 완전히 대체할지 범위 확인이 필요합니다.

---

## 5. 다음 단계 (승인 필요)

CLAUDE.md 규정에 따라 실제 코드/스키마 생성 전, 아래 범위에 대한 승인을 요청드립니다.

1. 위 아키텍처/기술스택 확정 여부
2. 신규 Firebase 프로젝트 생성 여부 vs 기존 `crosswb-a7083` 재사용
3. Git 저장소 연결 여부 (`wbmanager.git`)
4. 우선 구현할 모듈 순서 (예: 기준정보 → 입출고 → 재고 → 정산/손익 순 제안)
